name: rodarDia

on:
  push:
    branches:
      - main
  schedule:
    - cron: '0 12 * * 2'  

jobs:
  dia:
    runs-on: ubuntu-latest

    steps:
    
      - name: rodouDia
        run:  |
          varDia=$(TZ="America/Sao_Paulo" date '+%Y-%m-%d')
          echo "Rodado no dia $varDia"
              
          
